{
  "name": "portfolio-ask-ai-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that answers questions about Vinay Panwar via the Claude API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.121.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.0.0"
  }
}
